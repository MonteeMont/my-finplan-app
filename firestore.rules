rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // This rule allows a user to access documents in the 'budget' 
    // collection where the document ID is *exactly* their user ID.
    match /budget/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}